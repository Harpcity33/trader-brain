# Titan full-live policy map — September 8, 2026 target

This map separates verified current policy from staged repository material and
from engineering-only synthetic values. Missing authority is never interpreted
as permission, and a staged limit is never promoted merely because code can
enforce it.

| Control | Authoritative evidence | Runtime treatment | Resolution |
|---|---|---|---|
| Account | Authenticated read-only Robinhood evidence in `CAPABILITIES.json`: sole accessible account ending 7153; individual, limited-margin cash-management type; active at the recorded instant | Release and durable runtime are bound to `ending-7153`; a different account/type fails closed | Verified for the recorded instant; every run still requires fresh broker evidence |
| Strategy identity | Active `automations/robinhood-momentum-engine/automation.toml` | Bound to `titan_live_attended_compat_premarket_2026-08-27_v2`; full-live is a new runtime identity, not a change of strategy scope | Preserved |
| Instruments and direction | Active equity heartbeat plus `CODEX_FULL_LIVE_AUTONOMY_2026-09-08.md` | Exchange-listed Robinhood-tradeable long stock only, strictly above $5, whole shares; options, crypto, shorts, fractional shares, leverage/debit, ADD, re-entry, averaging down, stop widening, and overnight holdings disabled | Preserved; the rejected tiny-pilot share/count/dollar caps are not imported |
| Universe floor | `CODEX_FULL_LIVE_AUTONOMY_2026-09-08.md` and existing eligibility/scoring configuration | Session volume must be at least 750,000 over the documented session measurement; completed-bar causality remains mandatory | Preserved |
| Sessions | Active equity heartbeat | Premarket entry remains attended-only 07:00–09:25 ET; no new entry 09:25–09:35; regular entry 09:35–15:30; closeout begins ten minutes before the verified close and broker flatness is due five minutes before it | Preserved, with explicit holiday, DST, and early-close calendar gates |
| Entry orders | Active equity heartbeat and connector contract | Whole-share limit entry only; regular-hours GFD in the autonomous pipeline. Premarket is not autonomously promoted because native stop protection is unavailable before 09:30 | Preserved |
| Protection and exits | Active equity heartbeat and connector contract | Each confirmed fill delta creates a durable protection obligation. A regular-hours GTC stop-market is counted only after broker evidence says it is working. Exit capacity, cancel acknowledgement, cancel/fill races, and strictly newer reconciliation prevent overlap/oversell | Preserved; no atomic bracket/OCO capability is claimed |
| Daily entry lock | Active equity heartbeat | Irreversible account-day lock at broker-confirmed realized P&L of -$100 or lower | Verified and preserved |
| Profit goal/floor | Active equity heartbeat | +$150 is aspirational. After the first broker-confirmed crossing, proposed risk must preserve at least +$125 | Verified and preserved; never forces a trade |
| Percentage risk limits | `config/risk_limits.json` and `automations/robinhood-momentum-engine/DETERMINISTIC_RISK_OVERLAY.md`; `validation/IMPLEMENTATION_STATUS_2026-08-29.md` states the overlay is staged and not applied to the active equity prompt | The writer implementation can enforce the checked-in values, using the stricter percentage/absolute bound and positive reserve, but the signed full-live config sets `limits_live_provenance_verified=false` | **BLOCKED:** owner-approved live provenance/conflict resolution is not demonstrated; activation cannot promote these values |
| Spread and displayed depth | Active heartbeat requires acceptable spread/depth but supplies no numeric thresholds | `max_spread_bps` and `minimum_depth_multiple` are null and are hard activation/entry blockers | **BLOCKED:** exact approved numeric values are required; absence is not unlimited |
| Setup/execution score floors | Existing `config/scoring.json` defines component semantics and weights but no approved live score floors or A+ cutoffs | All four live threshold fields are null; synthetic test values exist only inside tests | **BLOCKED:** approved live floors and A+ cutoffs are required |
| Buying capacity | Fresh broker cash and unleveraged buying power; no current-balance value is embedded in policy | Every entry is capped by the lesser available cash/unleveraged buying power after open, pending, manual, and unknown reservations | Preserved; the point-in-time $912.80 audit value is evidence, not a permanent budget |
| Broker mutation authority | Authenticated connector capability audit in `CAPABILITIES.json` | Durable intent/review/submission code is implemented, but production requires a supported daemon client, whole-broker/advanced-order reads, client-reference reconciliation, and unattended writes. The current connector requires a current review and explicit user confirmation per mutation | **BLOCKED:** confirmations are not bypassed and connector credentials are not extracted |
| Local mutation authority | `config/full_live.json` | Independent interlock defaults false in addition to signed policy, durable activation, and the single writer lease | **BLOCKED/PAUSED:** only a reviewed replacement release may set it true after all other gates are satisfied |
| Market data | Existing local Titan Massive SQLite store and checked-in producer identity | Query-only compatibility reader consumes fresh shadow structures, quotes, and completed bars; shadow records never grant execution authority | Implemented; current freshness is session-dependent and required again at cutover |
| Tradability and independent validation | Active heartbeat requires current Robinhood tradability plus causal executable evidence | Provider interfaces and deterministic pipeline are implemented; the installed connector cannot supply these to a daemon, so provider fields are `unavailable` | **BLOCKED:** supported fresh providers must be wired and proven |
| Notifications | Full-live brief requires durable event notifications through an existing destination | Durable outbox, retry/dedupe, and fsynced local JSONL sink are implemented. No proven bridge from that sink to the existing Codex heartbeat exists | **BLOCKED:** a real destination delivery and receipt test is required; a local write alone does not count |
| Activation | Owner request authorizes implementation and a user-controlled procedure, not present order mutations or scheduler activation | Hash-bound, one-use, expiring activation record; exact phrase; only PAUSED→RECONCILING, followed by a fresh service-side account reconciliation before ACTIVE | Implemented but deliberately unsatisfiable under the current blocked signed config |

The offline tests temporarily replace blocked fields with explicitly labelled
synthetic values to exercise post-activation branches. Those values never
modify `config/full_live.json`, never become an install manifest, and carry no
live authority.

# Immutable Premarket Predictions

One create-only dated artifact per trading day. A Top list may contain fewer
than five candidates and must never be padded. Every serious candidate needs a
specific `setup_id`, hard-gate evidence, separate SETUP_SCORE and
EXECUTION_SCORE, provenance, thesis, levels, route comparison, risks, and an
explicit live-qualification or rejection reason.


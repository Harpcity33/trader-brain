# Local only: ~/.config/trader-brain/paper-options.env (mode 600).
# No paid OpenAI API key, broker password, or brokerage account access is used.
MASSIVE_API_KEY=
MASSIVE_API_BASE_URL=https://api.massive.com
TB_GMAIL_SENDER=
TB_GMAIL_APP_PASSWORD=
TB_GMAIL_RECIPIENT=
# Optional override; leave blank for the private default state location.
TB_STATE_PATH=
